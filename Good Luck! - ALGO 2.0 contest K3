Q1: K3  Algorithm Unlock 2.0 Question<br>
      Given a string s of size n. Remove all the characters in the string which are uppercase English alphabets, and print the resultant string.
//C++ Question
#include <iostream>
using namespace std;

int main() {
	// your
	int N;
	cin>>N;
	char a[N];
	for(int i=0;i<N;i++)
	  cin>>a[i];
	for(int i=0;i<N;i++)
	   if(a[i]>=97)
	      cout<<a[i];
	return 0;
}
